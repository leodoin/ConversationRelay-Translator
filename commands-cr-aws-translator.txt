====================================================
BEFORE FIRST DEPLOY
====================================================
1) => Install node libraries
npm --prefix ./layers/layer-cr-twilio-client/nodejs install

2) => Save Secrets to AWS SecretsManager

Use AWS Secrets Manager and create a secret for this stack. The secret MUST BE NAMED CR_APT_SEARCH and have the following SecretStrings:

TWILIO_ACCOUNT_SID
TWILIO_AUTH_TOKEN

====================================================
DEPLOY TEMPLATE
====================================================
Run `sam build` from this directory every time before deploying.

Run 
sam deploy --stack-name CR-TRANSLATOR --template template.yaml --profile $(cat ./aws-profile.profile) --capabilities CAPABILITY_NAMED_IAM

* The first time you deploy, add `--guided` (`sam deploy --guided --stack-name...`) to the above command to generate a configuration file. All subsequent commands do not need the --guided.

====================================================
LINK AWS REST API ENDPOINT TO TWILIO
====================================================
Take Output from the stack called "TwimlAPI" and assign it to the Webhook for Voice handler for their desired phone number.

====================================================
DATA LOADING (load profiles for Caller and Callee)
====================================================
=> Caller English
aws dynamodb put-item --table-name CR-TRANSLATOR-AppDatabase --item "$(node ./configuration/dynamo-loaders/profile-caller-example-en.js | cat)" --profile $(cat ./aws-profile.profile)

=> Caller Spanish
aws dynamodb put-item --table-name CR-TRANSLATOR-AppDatabase --item "$(node ./configuration/dynamo-loaders/profile-caller-example-es-MX.js | cat)" --profile $(cat ./aws-profile.profile)

=> Agent (Callee) English
aws dynamodb put-item --table-name CR-TRANSLATOR-AppDatabase --item "$(node ./configuration/dynamo-loaders/profile-agent-callee-example-en.js | cat)" --profile $(cat ./aws-profile.profile)

=> Agent (Callee) Spanish
aws dynamodb put-item --table-name CR-TRANSLATOR-AppDatabase --item "$(node ./configuration/dynamo-loaders/profile-agent-callee-example-es-MX.js | cat)" --profile $(cat ./aws-profile.profile)